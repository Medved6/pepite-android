<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_search"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.sergewsevolojsky.pepiteapp.activity.SearchActivity">


    <LinearLayout
        android:id="@+id/top_block"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:background="@color/colorMain"
        android:gravity="center"
        android:focusable="true"
        android:focusableInTouchMode="true">

        <EditText
            android:id="@+id/sport_edit_text"
            android:layout_width="280dp"
            android:layout_height="36dp"
            android:hint="Faire une recherche"
            android:paddingLeft="22dp"
            android:background="@drawable/searchshape"
            android:alpha="0.3"
            android:nextFocusUp="@id/sport_edit_text"
            android:nextFocusLeft="@id/sport_edit_text"/>

    </LinearLayout>


    <ListView
        android:id="@+id/search_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/top_block"
        android:paddingBottom="72dp">


    </ListView>

    <include
        layout="@layout/activity_bottom_bar"
        />

</RelativeLayout>
